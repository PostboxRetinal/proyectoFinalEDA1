function calcularScoreBoard(caso) {
    let penaltyTime = 0;
    if (caso no resuelto) {
       
        if (caso === "I") {
            penaltyTime += 20
        } 
        
    } 
    return caso;
}

export default calcularScoreBoard;